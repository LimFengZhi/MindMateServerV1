# ===========================================================================
# MindMate (Supabase multi-agent mental-health companion) — runtime deps.
#
# PyTorch is intentionally NOT pinned here: install the build that matches the
# target machine FIRST (only needed when AI_MODE=real).
#   CPU only:        pip install torch --index-url https://download.pytorch.org/whl/cpu
#   GPU (CUDA 12.1): pip install torch --index-url https://download.pytorch.org/whl/cu121
# ===========================================================================

# --- Web framework ---
Flask>=3.0
Flask-Limiter>=3.5          # optional; app degrades gracefully without it

# --- Config (.env secrets + config.yaml app tuning) ---
python-dotenv>=1.0
PyYAML>=6.0

# --- Database / Auth ---
supabase>=2.0              # Supabase Python client (Postgres + GoTrue auth)

# --- Resource importer (scripts/scrape_resources.py) ---
requests>=2.31
beautifulsoup4>=4.12

# --- Staff session-analysis report PDF (app/admin/pdf_report.py) ---
fpdf2>=2.7

# --- Agent orchestration (the chat pipeline is a LangGraph state machine) ---
langgraph>=1.0
langchain-core>=1.0

# --- AI / inference (only loaded when AI_MODE=real) ---
transformers>=4.44
accelerate>=0.30
peft>=0.11
# ONNX classifier support (the deployed int8 RoBERTa export; registry
# auto-detects a *.onnx file in CLASSIFIER_PATH and loads it through optimum)
optimum[onnxruntime]>=1.20

# --- Voice (speech-to-text) ---
faster-whisper>=1.0

# --- Production WSGI server ---
gunicorn>=22.0


runpod