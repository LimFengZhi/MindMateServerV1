.env
.venv/
__pycache__/
*.pyc
models/
*.log
.claude/settings.local.json

# --- fine-tuning study ---
# The study is its OWN repository, cloned into this folder (its README
# documents why it must sit here: it reads ../data, ../models and the app's
# config by relative path). This repo ignores it entirely.
fine_tuning_chatbot/
# data and model weights are NOT git-tracked
/data/
checkpoints/
*.safetensors
*.bin
.ipynb_checkpoints/
.vscode/
test_input.json
docker/docker-compose.web.yml
